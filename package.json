{
  "name": "jump",
  "version": "0.1.0",
  "devDependencies": {
    "@types/jsdom": "^28.0.0",
    "happy-dom": "^20.7.0",
    "jsdom": "^28.1.0",
    "linkedom": "^0.18.12",
    "typescript": "^5.0.0",
    "web-ext": "^9.3.0"
  },
  "scripts": {
    "build": "bun build src/content.ts src/background.ts src/detect-listeners.ts src/inject-detect-listeners.ts --outdir dist --target=browser --format=iife",
    "dev": "bun build src/content.ts src/background.ts src/detect-listeners.ts src/inject-detect-listeners.ts --outdir dist --target=browser --format=iife --watch",
    "package": "web-ext build --overwrite-dest"
  },
  "type": "module"
}
